const anyLetter = (letter) => { try { eval(letter) } catch (e) { return (e + e)['0'.charCodeAt()] } }
const rangeError = [][1];
const undefinedAlt = Math.PI[-1]
const undefinedLetter = (number) => `${undefinedAlt}`[number].toUpperCase()
